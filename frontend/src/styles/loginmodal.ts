import styled from "@emotion/styled";
import { Button as BaseButton, Modal as BaseModal } from "antd";

export const Button = styled(BaseButton)`
	&& {
		width: 100px;
	}
`;

export const Modal = styled(BaseModal)``;
